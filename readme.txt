Python Libraries:
Data manipulation and analysis:
pandas==1.3.3
numpy==1.21.2
Machine learning and model training:
scikit-learn==0.24.2
Deep learning:
tensorflow==2.6.0
Visualization:
matplotlib==3.4.3
seaborn==0.11.2
Utilities:
jupyterlab==3.1.12

import the libraries shown above
use python 3.8 for this project
load the datasets
print head of the datasets
print number of columns in dataset
input the columns array and assign the columns to each datasets.
handle the missing values
use one hot encoder to encode into categorical features
split the dataset into features and labels
encode the lables
normalize the features
create the models for each algorithm
train and test them on the datasets
compute the classification report and confusion matrix
compare the accuracies and finalize the best model
